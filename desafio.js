//5 Variáveis para jogador com interpolação
let nomeJogador = "Alan";
const nickJogo = "Joel";

const nomeJogo = "The last of us";
let objetivo = "Levar carga";
let recompenca = "ficha de comida";

console.log(`${nomeJogador} jogou ${nomeJogo} com o personagem ${nickJogo} seu objetivo era ${objetivo} e pretendia obter ${recompenca}`);

let balas = 32;
let inimigo = 19;

let derrotar = balas - inimigo;

console.log(`${nomeJogador} enfrentou ${inimigo} inimigos e sobraram ${derrotar} balas`);

let ponto1 = 300;
let ponto2 = 500;
let totalPontos = ponto1 + ponto2;
console.log(`Você tem ${totalPontos} pontos`); 

function verificarPontos(totalPontos) {
    if ( > 1.000){
        return "Você subiu de nível"
    }
}

let nivel = 9;
